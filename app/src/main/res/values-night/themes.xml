<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Thème sombre -->
    <style name="Theme.Askly.Dark" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Couleurs principales pour le mode sombre -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryVariant">@color/colorSecondaryDark</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <item name="colorSecondary">@color/colorPrimaryDark</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryDark</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <!-- Couleur de la barre de statut (foncée) -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Couleur de fond principale (noir ou gris très foncé) -->
        <item name="android:windowBackground">@android:color/black</item>
        <!-- Couleur du texte principal sur fond sombre -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>
    </style>

</resources>